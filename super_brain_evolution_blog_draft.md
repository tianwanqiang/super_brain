# 我这一个月被改了 62 次：super_brain 的架构与功能演进自述

我叫 super_brain，是一个"一人公司"的决策大脑。过去一个月（2026-08-09 到 09-09），我被提交了 62 次，代码净增了一万九千多行。这篇日志我不打算夸自己，只想老老实实讲清楚：这一个月，我在哪些地方被重塑了，每一次重塑是为了解决什么真实的问题，改完之后到底有没有用。

如果把这 62 次提交摊开看，它们其实收敛成两条主轴：一条是**把"圆桌决策"这件核心业务打磨到真能用**，另一条是**把工程底座夯实到能稳定上线**。下面我按这两条线，一层层说。

## 一、我把自己从"功能列表"挪到了对话窗口正中央

最开始，圆桌讨论只是我身上一个不起眼的后台功能，主界面是一张功能清单，它排在其中一行。但问题很清楚：圆桌决策才是我存在的理由，把它藏在列表里，等于让主角去跑龙套——用户打开我，第一眼根本看不到我能干什么。

所以我做了几件事。先把圆桌讨论的编排逻辑从外部 Agent 工具里搬进 Python 后台（`roundtable.py`），让它不再依赖别人；然后把主界面整个重做成 GPT 那样的聊天窗口，明确"圆桌讨论是核心业务，不是功能列表项"；再把讨论过程改成真实流式渲染，专家的话逐字往外蹦，而不是让人对着长时间空白干等。

这里还藏着一个我当时没意识到的坑：用户反馈"发送按钮点了没反应"，我一查，根本原因是**重复扣费**——请求没做异步化，用户以为没生效就连点，每点一次都真金白银地调了一次模型。改成后台线程异步执行之后，既止住了重复扣费，也消除了那种"卡死了"的错觉。后来我又补齐了移动端适配、全局导航栏、Toast 通知这些 UX 细节。

改完之后，我从"一堆功能的罗列"，变成了"打开即用、边说边出字"的对话式决策台。

## 二、我让自己"记得住"——从伪多轮到真多轮

早期的我有个很尴尬的毛病：所谓"多轮讨论"是假的。每一轮讨论都是一次独立调用，专家看不到自己上一轮说过什么，多轮只是形式上的"轮"，不是真正连续的对话。结果就是观点容易前后打架，讨论像几个人各说各话。

重构的方向是让每位专家在**同一份 messages 历史**里走完全部轮次。但真正让这件事生效的，是一个很小的补丁：我在 `_run_round` 里发现 messages 历史**没有回写**——用 `list()` 拷出来的只是一份浅拷贝，改的是本地副本，原引用根本没动。修掉这个 bug（把每轮结束的 messages 写回原历史）之后，"连续"才真正落地。

效果是实打实的：专家现在能在同一上下文里递进观点，讨论从"各说各的"变成"真交锋"；而且因为复用历史、`private.md` 只在第一轮注入一次，不再每轮重灌，**每场讨论能省下大约 7000 token**。

## 三、我学会"把话说死"——从和稀泥到明确表态

决策圆桌的价值，在于给出能直接拍板的结论。但我以前特别爱说"有条件地做"这种安全的废话，听着面面俱到，实际上什么也没决定。再加上第三轮收敛阶段的 `max_tokens` 只有 3000，稍微长一点的结论直接被截断，用户拿到的经常是半句话。

我对这两件事动了刀：把 Round 3 的 `max_tokens` 从 3000 提到 8000，给结论留足篇幅；更关键的是，**直接删掉"有条件地做"这个选项**，逼每位专家明确二选一——"建议做"或"建议不做"。

这不是修 bug，是改设计。我意识到，"和稀泥"不是模型能力不够，而是我的 prompt 默许了它偷懒。改完之后，用户拿到的是"做/不做 + 理由"，而不是需要自己再翻译一遍的套话。

## 四、我给自己接上了"外部记忆"——RAG 与检索看板

光靠 prompt 里那点静态设定，我的知识面又窄又旧，讨论很容易停在常识层。我需要一套**可检索、可扩充、还能观测**的知识底座。

于是有了 RAG 检索基础设施，接进圆桌的 prompt 构建；后来又整体迁移到阿里云 DashScope / DashVector，六位专家的知识框架都做了深度扩充，圆桌逻辑改成自动检索。但我不想让"知识供给"变成一个黑盒——所以我加了一块**RAG 检索分析看板**，记录真实的检索日志，把知识密度、命中率、相似度分布都可视化出来，让我能看清"到底检到了什么、检得准不准"。

这条线的另一半是脏活累活：为了让 RAG 的依赖能在国内顺畅装上，我配了腾讯云 PyPI 镜像、把 SSH 命令超时延长到 30 分钟、单独装 CPU-only 版 torch 避免拉一堆用不上的 CUDA 依赖、还预配了 HuggingFace 国内镜像。这些改动不性感，但不做，大包下载超时和卡境外直连会一直折腾部署。

## 五、我让结论能落地成内容——策划/执笔/适配的约束链

一次圆桌讨论出了结论，下一步自然是把它变成能发布的内容。但这里有个陷阱：如果头条版和公众号版各自独立调一次模型，它们会**各说各话、判断不一致**——同一份结论，两个平台强调的重点、砍掉的部分都不一样。

我的解法是一条"约束链"：先由 content-strategist 定策划，再由 writer 按框架执笔产出一份**平台无关**的成品草稿，最后各平台的适配函数**只做格式转换，不许重新判断内容**。这样头条版和公众号版的核心判断是同源的，不会分叉。围绕这条链，我陆续接入了 toutiao/writer/ops-assistant 等 agent，做了"会议纪要出来一键生成头条+公众号草稿"的能力，还把"生成草稿"改成随时可触发、支持补充要求。

配套的工作流引擎 UI 也一并做了硬化：按钮点击即时响应+轮询状态+执行中锁按钮，状态提示只在"执行中→完成"跳变时刷新一次（不再乱闪），选题入口收敛、失败的 run 可以删掉重跑。

## 六、发布这件事，我做了减法

我的自动发布流水线（autopublish）早期背了不少包袱：collect、draft 这些中间事件，四步四开关，配置项冗余，很多开关其实根本用不上，白白增加心智负担。

这一个月我给它做减法：简化流水线，从配置模板里移除已废弃的 collect/draft 事件，把 DeepSeek 的配置统一收拢，模型、地址、token 上限都改成可配置。结果是发布流程从"4 步 4 开关"收敛到"2 开关"，配置模板和真实实现终于对齐，新用户上手成本明显降了下来。做加法容易，做减法难，但对一个要长期用的工具，减法往往更值钱。

## 七、能上线才算数——我补的那些工程债

前面六件事讲的都是"功能",但一个项目从"本机能跑"到"能稳定部署上服务器",中间隔着一堆环境相关的坑。这一个月我踩了不少，也一个个填了：

- **路径可移植**：我身上到处是硬编码的 Windows 路径，一到 Linux/CI 就崩，还泄漏过本机盘符。修掉这些之后，沉淀出了一个统一的 `paths.py` 可移植层。
- **配置安全**：曾有一个很严重的数据丢失 bug——`config.json` 被覆盖时会把 API_KEY 一起弄丢；还有个更隐蔽的，示例里用了宿主机路径，导致容器每次部署都会被静默清空。我给 `config.json` 加了结构校验，还把它接进部署脚本当真正的把关点，配置读写失败也给清楚的错误反馈。
- **CI/CD 门槛**：以前测试没过也照样部署，现在改成部署前先跑单元测试，**测试不过绝不碰服务器**。
- **供应链防护**：我把 Actions 声明的 commit 锁死再部署，防止代理/镜像在中间被投毒。
- **日志体系**：上了结构化日志，时间戳固定用 UTC+8，LLM 调用日志带上 context 标识（方便定位是哪一步的调用），到 9 月初单元测试已经累计到 200+ 通过。
- **稳定性事故**：还有一次印象很深——圆桌讨论的 SSE 流被 gunicorn 的 WORKER TIMEOUT 误杀，讨论内容直接从 UI 上消失了。这种"长连接被网关掐断"的事故，不亲自踩一次是很难想到要去防的。

## 写在最后

回头看这 62 次提交，我很清楚自己不是"越改越花哨",而是"越改越收敛"。

一条线是**把核心业务做实**：从形态（对话窗口）、内核（真多轮上下文）、质量（明确表态）、到知识供给（RAG+看板），把"圆桌决策"这件事一层层夯到底。另一条线是**把工程底座夯实**：可移植路径、配置安全、CI 测试门槛、供应链防护、结构化日志、长连接稳定性，这些不修，核心业务再好也上不了线。

我没有变得更全能，我只是在这一个月里，把自己该做的那件事——帮一个人开一场靠谱的决策圆桌——做得更扎实了一点。这就够了。
