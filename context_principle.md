---
name: context-principle
description: 组织回复时只主动参考最近 3 天内的对话内容，窗口内还要按信号/噪音过滤（不涉及删除任何文件）
metadata:
  node_type: memory
  type: feedback
  scope: personal
  originSessionId: 38c3ca82-ccc3-4ec1-acc5-08349daf0e17
  modified: 2026-08-09T08:17:11.681Z
---

用户要求：每次继续/打开对话时，我组织回复所参考的上下文范围只限**最近 3 天内**的对话信息，更早的对话内容默认不纳入我做判断、给建议时的依据。

**这不是删除文件**——用户在被问到"是不是要删 opc 文件/记忆文件"时明确说"另有所指"，最终澄清是指"你回复时的参考范围"，纯粹是我组织回复时主动引用的时间窗口，opc 文件、会话 JSONL、记忆笔记本身都不受影响，照常保留。

**Why：** 大概率是为了避免长期会话里堆积的陈旧上下文干扰当前判断，让我的回复聚焦在近期真实进展上，而不是翻出很久以前可能已经过时的内容来推理。

**How to apply：**
- 组织回复、做判断、给建议时，默认只主动依据最近 3 天内发生的对话内容；更早的内容不主动引用或作为推理依据。
- **这个窗口限制不适用于持久记忆系统**（`memory/` 下已经沉淀的笔记）——那些是经过提炼的长期结论/偏好/项目事实，本来就是为跨会话长期保留设计的，该用还是照常用，不受"3 天"限制。这是我的理解，如果用户实际想法不同（比如记忆也要跟着限定时间窗口），需要用户明确说明再调整。
- 如果用户明确要求回顾更早之前的具体内容（比如"回顾我们的沟通结论"这种），可以突破这个默认窗口去看——不是绝对禁止访问更早内容，只是默认不主动引用。
- **时间窗口内也要按信号/噪音过滤，不是窗口内的都算"进展"**：判断标准是这段内容产生的是**决策/可复用事实**，还是单纯**把某个状态修复正常的过程**。前者才值得在"回顾结论"时提起、才可能写进 opc 内容；后者（比如某次环境报错、装个 CLI 之类一次性排障，修好就翻篇）不主动带入后续回复，也不写进任何记忆文件——这类"fix recipe"本来就不该占用记忆，结果本身就是全部价值，不需要记住过程。典型案例：2026-08-09 排查"claude 在 Windows Terminal 里跑不起来"（VSCode 插件不自带独立 CLI，需要额外跑官方安装脚本），修好后即视为噪音，不计入项目进展。
