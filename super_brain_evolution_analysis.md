# super_brain 改动历史统计分析（2026-08-09 → 2026-09-09）

> 本文件是"改动历史汇总"，只做客观统计与归因，不是对外文案。
> 数据来源：本地 git 仓库全量提交历史（`git log`）。所有 commit 短 hash 均可在仓库核验。

## 一、统计口径

| 指标 | 数值 |
| --- | --- |
| 时间窗 | 2026-08-09 → 2026-09-09（约 1 个月） |
| 提交总数 | 62 |
| 累计代码变动 | +19482 / -3317 行 |
| 涉及文件（去重） | 90+ |

**改动最频繁的文件（churn Top，剔除 .gitignore / inbox.md 等非业务文件）：**

| 文件 | 变更次数 | 角色 |
| --- | --- | --- |
| `ui_app.py` | 29 | Flask 后端总入口，几乎所有功能都要过这里 |
| `templates/index.html` | 18 | 主界面（圆桌对话窗口） |
| `roundtable.py` | 14 | 圆桌讨论编排内核 |
| `templates/admin.html` | 13 | 管理/配置界面 |
| `dispatcher.py` | 13 | 任务分发 |
| `.github/workflows/deploy.yml` | 13 | CI/CD 部署流水线 |
| `executors.py` | 8 | 内容生成执行器（策划→执笔→平台适配） |
| `agents.yaml` | 8 | agent 统一配置 |
| `paths.py` | 7 | 路径可移植层 |

**一句话结论**：改动高度集中在"主界面 + 圆桌内核 + 部署链路"三处，说明这一个月的主轴是**把核心业务（圆桌决策）打磨到能用、把工程底座（部署/配置/日志/测试）夯实到可上线**。

---

## 二、七条演进主线

每条主线按"概念原因 → 做了什么（挂真实 commit）→ 改进效果"三段式说明。

### 主线 1：主界面形态——从"功能列表"到"GPT 式对话窗口"

- **概念原因**：最初把圆桌讨论当成后台功能之一，UI 是一张功能清单。但圆桌决策才是 super_brain 的**核心业务**，藏在列表里等于把主角当配角，用户第一眼看不到价值。
- **做了什么**：
  - `d4eef0e` 新增 `roundtable.py`，Python 后台直接编排圆桌讨论，不再依赖外部 Agent 工具；
  - `d5280b7` UI 接入真实圆桌（`/roundtable/run` 路由直调 `roundtable.py`）；
  - `7475809` **重做主界面**：圆桌讨论改成 GPT 类聊天窗口，明确"它是核心业务不是功能列表项"；
  - `237dd4d` 确立"@ 唤起是主入口，inbox+dispatcher 只服务真正有 executor 的助手类 agent"；
  - `644f280` 圆桌讨论真实流式渲染，专家文字逐字实时展示；
  - `c59b647` 修复"发送按钮无响应"其实是**重复扣费 bug**，改为后台线程异步执行；
  - `a8c2f7e` 全局 UX 改进（移动端 viewport、全局导航栏、Toast、专家配色、@提问仅最后一轮等）。
- **改进效果**：主界面从"功能罗列"变成"打开即用、边说边出字"的对话式决策台；异步化消除了重复扣费和"卡死"错觉。

### 主线 2：圆桌上下文——从"伪多轮"到"真多轮"

- **概念原因**：早期每一轮讨论是独立调用，专家看不到自己前面说过什么，多轮只是形式上的"轮"，不是真正的连续对话，结论容易前后打架。
- **做了什么**：
  - `7bb0e75` 圆桌讨论改为**多轮上下文连续**——每位专家在同一 messages 历史中完成全部轮次；
  - `d5ad4b2` 修复 `_run_round` 中 **messages 历史未回写**的 bug（这是让"连续"真正生效的关键补丁）。
- **改进效果**：专家在同一上下文里递进观点，讨论从"各说各的"变成"真交锋"；同时因为复用历史而非每轮重灌，**每场省下约 7000 token**。

### 主线 3：结论质量——把"和稀泥"改成"明确表态"

- **概念原因**：决策圆桌的价值在于**给出可执行的结论**。但模型倾向输出"有条件地做"这类安全的废话，且 Round 3 收敛阶段 `max_tokens=3000` 太小，长结论被截断。
- **做了什么**：
  - `468bb43` Round 3 `max_tokens` 从 3000 提升到 8000，给收敛结论留足篇幅；
  - `160aa44` **去掉"有条件地做"选项**，强制专家明确表态"建议做 / 建议不做"。
- **改进效果**：结论不再被截断，也不再模棱两可——用户拿到的是能直接拍板的"做/不做 + 理由"，而不是需要自己再翻译一遍的套话。

### 主线 4：知识供给——从"空谈"到"RAG 检索 + 可视化看板"

- **概念原因**：专家光靠 prompt 里的静态设定，知识面窄、更新滞后，讨论容易停留在常识层。需要给每位专家接上**可检索、可扩充、可观测**的知识底座。
- **做了什么**：
  - `07740bb` 新增 RAG 检索基础设施，接入圆桌 prompt 构建，marketing 先扩容增长黑客方法论；
  - `637054c` RAG 迁移到**阿里云 DashScope / DashVector**，六位专家知识框架深度扩充，圆桌逻辑改为自动检索；
  - `7bbd89d` 新增 **RAG 检索分析看板**——记录真实检索日志，可视化知识密度/命中率/相似度分布；
  - 依赖与镜像工程化：`8e30a30`（SSH 超时延到 30 分钟）、`f10c0d0`（腾讯云 PyPI 镜像）、`3db6281`（CPU-only torch）、`6708f8e`（HF_ENDPOINT 国内镜像）。
- **改进效果**：专家发言有真实检索到的资料支撑；看板让"知识供给够不够、命中准不准"从黑盒变成可量化指标；国内镜像链路解决了大包下载超时/卡境外的部署顽疾。

### 主线 5：内容生产——"策划→执笔→平台适配"约束链 + 工作流引擎

- **概念原因**：一键把圆桌结论变成可发布内容时，如果每个平台各自独立调一次模型，头条版和公众号版会**各说各话、判断不一致**。需要一条"内容判断只做一次、平台只做格式转换"的约束链。
- **做了什么**：
  - `699bc9e` 新增腾讯云 Docker 部署 + **内容生成约束链条重构**（content-strategist→writer→平台适配）；
  - `be0831d` 新增 toutiao/writer agent，`dispatcher.py` 接入真实发布执行能力；
  - `a9b01a4` 新增 ops-assistant（读取当天内容分发到头条+公众号生成草稿）；
  - `05ca7ae` 新增 Round3 决策收敛/任务系统/专家私聊/待办/定期复盘闭环，接入 content-strategist 与两位新专家；
  - `f17dd7b` 会议纪要出来后可一键调助手 agent 生成头条+公众号草稿，agent 名字加中文翻译层；
  - `cf18e8a` "生成草稿"随时可触发（不再因为生成过就藏按钮），支持补充要求；
  - 工作流引擎 UI：`192708c`（按钮异步化：点击即响应+轮询+执行中锁）、`62c3432`（状态提示只在执行中→完成跳变时刷新一次）、`492d2eb`（选题入口收敛+失败可删/重跑+UI 简洁化）。
- **改进效果**：同一份素材生成的头条版与公众号版**核心判断一致**（下游只做格式转换，不重新判断内容）；工作流页面从"点了没反应/结果乱闪"变成"即时反馈 + 状态可控 + 失败可重跑"。

### 主线 6：发布自动化——autopublish 流水线做减法

- **概念原因**：autopublish 早期有 collect/draft 等中间事件、四步四开关，配置项冗余、心智负担重，很多开关实际用不上。
- **做了什么**：
  - `c9d9749` 简化 autopublish 流水线；
  - `79dc690` 更新 `config.json.example`，移除已废弃的 collect/draft 事件；
  - `14f3435` DeepSeek 配置统一收拢到 super_brain，模型/地址/token 上限改成可配置。
- **改进效果**：发布流程从"4 步 4 开关"收敛到"2 开关"，配置模板与真实实现对齐，新用户上手成本显著下降。

### 主线 7：工程硬化——可移植路径 / 配置校验 / CI 测试门槛 / 供应链防护 / 结构化日志

- **概念原因**：项目从本机能跑，到要稳定部署上服务器，暴露出一批"环境相关"的隐患：硬编码 Windows 路径、config.json 被覆盖丢 key、CI 无测试就部署、代理/镜像供应链投毒、日志时间戳混乱、SSE 被 gunicorn 误杀。这些不修，核心业务再好也上不了线。
- **做了什么**：
  - 路径可移植：`9d86fe2`（修 ui_app.py/publishers.py 硬编码 Windows 路径）、`fa6a96a`（修 CI/Linux 路径 + 移除本机盘符泄漏）、`paths.py` 累计 7 次改动沉淀出可移植层；
  - 配置安全：`7803d19`（修 config.json 覆盖丢失 API_KEY 的数据丢失 bug + 日志路径硬编码，新增单元测试套件）、`7e4c25a`（config.json 结构校验，接入部署脚本作为把关点）、`d7e3ff2`（修 MEETING_MINUTES_DIR 用宿主机路径导致容器每次部署被静默清空）、`68a4c48`（config/set 接口加操作结果反馈）；
  - CI/CD 门槛：`1be24fd`（部署前先跑单元测试，测试不过不碰服务器）；
  - 供应链防护：`52eaaa8`（锁定 Actions 声明的 commit 部署，防代理/镜像投毒）；`6af3ead`/`842dd00`/`efd94fd`/`97ebbff`（改 Actions 直推、rsync 参数与属主修复、fetch 重试）；
  - 日志体系：`36e7386`（结构化日志）、`2402722`（时间戳固定 UTC+8）、`a6b1a88`（LLM 调用日志带 context 标识，200 测试通过）、`4a6468c`（重构日志格式 + 清理 dispatcher/inbox 机制）；
  - 稳定性事故修复：`9209125`（修圆桌 SSE 流被 gunicorn WORKER TIMEOUT 误杀、讨论内容从 UI 消失）。
- **改进效果**：从"本机偶然能跑"进化到"CI 有测试门槛、部署锁定 commit、配置有校验、日志可追溯、长连接不被误杀"的可上线状态；单元测试套件累计到 200+ 通过。

---

## 三、演进的方向（定调）

这 62 次提交，收敛成两条主轴：

1. **核心业务打磨**：把"圆桌决策"这件事从形态（对话窗口）、内核（真多轮上下文）、质量（明确表态）、知识供给（RAG+看板）四个层次逐层做实——这是 super_brain 存在的理由。
2. **工程硬化**：把可移植路径、配置安全、CI 测试门槛、供应链防护、结构化日志、长连接稳定性逐一补齐——这是 super_brain 能持续在线、可信交付的底座。

内容生产链（主线 5）与发布自动化（主线 6）是连接"决策"与"落地"的桥：让一次圆桌讨论的结论，能被约束链一致地转化成多平台可发布内容，并经精简后的流水线推送出去。

> 基调：用真实 commit 和真实数字说话，克制、不吹。这份分析是后续第一人称技术博文的**事实底稿**。
