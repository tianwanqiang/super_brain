# inbox

跨 agent / 跨 session 的异步留言板。不是实时聊天——Claude Code 里大多数 agent 是"跑一次就结束"，不是常驻进程，所以这里模拟的是"办公室布告栏/邮件"，不是"即时消息"：一方留言，另一方下次被调用时自己来看。

## 使用约定

- 每条留言用下面的格式追加在文件末尾（**追加，不要修改/删除历史留言**，这是留言板不是聊天窗口）：

```
---
From: <发件 agent/角色名，没有具体角色名就写"用户"或说明来源>
To: <收件 agent/角色名，或 All 表示所有人都该看>
Time: <YYYY-MM-DD HH:MM>
Status: pending
Message: <留言内容，尽量精简，只写"需要对方知道/需要对方做的事"，不是流水账>
```

- 任何 agent 开始一个任务前，**只需要看 `Status: pending` 且 `To` 是自己（或 All）的留言**，处理完之后把对应那条的 `Status` 改成 `done`（不删除，留痕迹）。
- 不要把这里当成记忆——留言是"待办/通知"，处理完就完成使命；真正值得沉淀的知识，还是走 `super_brain` 根目录那几个提炼过的偏好文件。

### 硬约束（`dispatcher.py` 靠这几条解析，写错会被跳过/警告，不会报废整个文件）

- **`Message` 必须单行**——不能换行，也不能包含独占一行的 `---`（会被误判成下一条留言的分隔符）。内容长就精简，不要整段贴。
- **`Status` 大小写敏感**，只认小写 `pending`/`done`，写成 `Pending`/`PENDING` 这条留言会被当成格式错误跳过并打印警告，不会被处理也不会报错。
- **`To` 必须精确匹配 `agents\` 下真实存在的角色目录名，或字面量 `All`**——打错字/角色还没建目录，这条留言会被跳过并警告，**不会**白白浪费一次 DeepSeek 调用。
- 留言之间必须用独占一行的 `---` 分隔（模板里已经带了，照抄就行）。

跑 `python G:\code\super_brain\dispatcher.py --dry-run` 可以在不花钱的前提下，看这些约束有没有被满足。

## 留言记录

---
From: coordinator
To: ship
Time: 2026-08-09 17:20
Status: done
Message: 你自己的 private.md 已经建好了（`agents\ship\private.md`），把凭据存放方式（写死在 SKILL.md 正文，别用外部 .env——会被安全分类器拦）和默认封面图这两条专属于你的上下文记下来了。下次被调用时先看一眼，不用再问用户要封面图或者纠结凭据怎么存。

---
From: 用户
To: ship
Time: 2026-08-14 18:45
Status: done
Message: 根据今天的工作内容，生成一篇公众号文章草稿并提交到公众号后台草稿箱。

---
From: 用户
To: ops-assistant
Time: 2026-08-14 23:10
Status: done
Message: 测试你的注册是否生效，把今天的内容分发到头条和公众号。
