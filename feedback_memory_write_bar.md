---
name: feedback-memory-write-bar
description: "不要把\"这次窗口讨论出的东西\"自动当成可复用原则写进持久记忆——只有用户明确判断\"这个可复用\"才写"
metadata: 
  node_type: memory
  type: feedback
  scope: personal
  originSessionId: 38c3ca82-ccc3-4ec1-acc5-08349daf0e17
  modified: 2026-08-09T08:17:36.051Z
---

用户纠正：2026-08-09 我把一次对话里提出的"主任务/次要任务"框架，当场当作可复用原则写进了 `context_principle.md`（还同步进了 `super_brain`）。用户指出：`context_principle.md` 这类持久记忆文件**不需要跟每次沟通成果实时同步**，那次说的"这个上下文窗口的任务"本身就是**限定在这个窗口**的描述，不是要我永久记住的规则——除非用户自己判断"这些东西可以复用"，才应该写进去。

**Why：** 我倾向于把"听起来像框架/原则"的内容自动升级成持久记忆，但用户说话时的范围限定（"这个上下文窗口的任务"）本身就已经明确了它不是要长期生效的规则。把范围限定的内容当成无限定的通用规则来记，是过度沉淀。

**How to apply：**
- 听到用户描述一个框架/原则/判断标准时，先看措辞里有没有范围限定（"这次""这个窗口""这一次任务"这类）——有限定词的，默认只在当前上下文里适用，不要自动写进 `memory/`。
- 只有用户明确说"这个可以记下来""以后都这样"之类的话，或者内容本身完全不带"这次/这个"这类限定语气，才考虑写入持久记忆。
- 不确定要不要写的时候，先问，不要因为"反正记忆文件可以再改"就先写了再说——这条本质上是 [[feedback_confirm_before_meta_execution]] 在"要不要写记忆"这个具体场景下的延伸。
