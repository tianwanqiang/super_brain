---
name: feedback-ui-product-thinking
description: 用户对网站/产品 UI 设计的两条硬性准则——不允许用简单文字链接堆砌功能入口，且每次加新功能要重新设计整体布局
metadata: 
  node_type: memory
  type: feedback
  scope: personal
  originSessionId: 93c16af2-b98d-4590-8277-8f8d4955aab6
  modified: 2026-08-09T06:26:03.383Z
---

做 metahub（AIGC 智能体工作台）网站时，用户明确提出两条设计准则：

1. **页面元素设计必须符合产品思维，用户友好，不允许放置简单链接。**
   不能把功能入口做成一排 `<a>` 纯文字链接（哪怕加了颜色），要有图标、卡片、按钮等
   真正的视觉层级和交互反馈，让人一眼看出"这是个功能入口"而不是"这是一段带链接的文字"。

2. **每次新增功能，都要重新设计页面布局，让整体符合用户使用习惯、贴合互联网产品特点。**
   不能图省事，把新功能的入口简单地追加到已有的列表/导航末尾了事（比如往侧栏 footer 的
   链接列表里再加一行）。要重新审视整体信息架构：这个新功能该放在主导航还是二级入口？
   会不会和已有入口重复/冗余？要不要重新分组？

**Why**：用户是把这个项目当真正要商业化的产品在做（订阅制、多模型、自定义智能体、AI 图
片/视频生成等功能陆续在加），不是一个功能堆砌的技术演示。之前的实现（sidebar-footer-links、
topnav-links）就是"一排纯文字链接"的反面案例，被用户明确点名要改。

**How to apply**：
- 以后每次给这个项目加新页面/新功能入口，先想清楚它在整体导航结构里的位置，而不是找个
  最近的列表加一行。
- 具体元素：优先用带图标的按钮/卡片/pill 式导航项，避免裸露的 `<a>` 文字链接作为主要
  功能入口（次要的、正文里的辅助链接除外，比如"了解更多"这种可以是普通链接）。
- 这条准则不只适用于 metahub，是这个用户对"网站类项目"的通用审美/产品要求，其他类似
  项目也应该默认适用，除非用户另有说明。
