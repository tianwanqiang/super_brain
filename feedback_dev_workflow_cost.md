---
name: feedback-dev-workflow-cost
description: 开发新功能时的工作流顺序——先把逻辑闭环写完/静态验证，再启动服务，交给用户人工测试
metadata: 
  node_type: memory
  type: feedback
  scope: personal
  originSessionId: 93c16af2-b98d-4590-8277-8f8d4955aab6
  modified: 2026-08-09T06:26:13.088Z
---

**通用原则，不限于某个项目**：涉及会真实调用第三方付费 API（不管是哪个项目、哪个厂商——聚合视频生成 API、火山引擎 Ark、DeepSeek、微信等任何按调用计费的接口）的功能，开发顺序必须是：

1. 先把代码逻辑写完整（provider + 路由 + 模板 + JS 全部打通）。
2. 用不触发真实网络请求的方式做静态验证（`python -c "import ..."` 检查语法/实例化、Jinja `render_template` 检查模板渲染、纯 Python 逻辑单测用真实抓到的历史响应样本模拟分类结果等）。
3. 只启动/重启一次服务，交给用户在他们自己的环境里做真实的人工测试（用户会自己跑 `debug_video_api.py` 或在网页上操作）。

**Why**：用户在 2026-08-04 明确说"由于费用紧张，以后所有的功能先完成逻辑闭环再运行服务，最后人工测试"——每次为了验证一个小改动就重启服务、甚至跑会触发真实第三方 API 调用的脚本（如 `debug_video_api.py`），会产生真实费用。之前的做法是每改一处就重启一次 Flask 服务（虽然重启本身不直接花钱，但配合频繁的真实 API 探测调用会产生不必要的开销），不符合用户的成本控制预期。

**How to apply**：
- 不要为了验证代码改动而反复重启 Flask 服务；把多个相关改动攒到一起，最后统一启动一次。
- 不要主动替用户跑会调用真实付费 API 的命令（如 `debug_video_api.py`、点击网页生成按钮对应的后端逻辑）——这类"真实调用"交给用户自己决定要不要花这个钱去测。我可以调用真实 API 去**验证纯粹的连通性/路径是否 404**这种低成本试探，但涉及真正生成内容（图片/视频）的请求应避免由我发起。
- 验证手段优先级：语法检查 > 模板渲染 test > 用已经真实抓到的历史响应样本做纯逻辑回放测试 > 万不得已才建议用户自己跑一次真实调用。
- 相关：[[feedback_ui_product_thinking]]（同一个项目里另一条关于 UI 设计的强约束）。
